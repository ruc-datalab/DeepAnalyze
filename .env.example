# ==============================================================================
# DeepAnalyze Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# Model Backend Selection
# ------------------------------------------------------------------------------
# Set to "true" to use OpenRouter API, "false" to use local vLLM server
USE_OPENROUTER=false

# ------------------------------------------------------------------------------
# OpenRouter Configuration (when USE_OPENROUTER=true)
# ------------------------------------------------------------------------------
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# Model to use (see full list at: https://openrouter.ai/models)
# Recommended models:
#   - anthropic/claude-3.5-sonnet (Best quality, $3/$15 per 1M tokens)
#   - anthropic/claude-sonnet-4.5 (Latest Claude model)
#   - deepseek/deepseek-r1 (Best value, $0.55/$2.19 per 1M tokens)
#   - openai/gpt-4o (Fast, $2.5/$10 per 1M tokens)
#   - google/gemini-2.0-flash-thinking-exp (Free with limits)
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# Optional: Your site URL (for rankings on openrouter.ai)
SITE_URL=https://github.com/ruc-datalab/DeepAnalyze

# ------------------------------------------------------------------------------
# Local vLLM Configuration (when USE_OPENROUTER=false)
# ------------------------------------------------------------------------------
# Path to your local model (must match the name when starting vLLM)
# Example: vllm serve DeepAnalyze-8B
MODEL_PATH=DeepAnalyze-8B

# vLLM API endpoint (default: http://localhost:8000/v1)
# API_BASE=http://localhost:8000/v1

# ------------------------------------------------------------------------------
# Workspace Configuration
# ------------------------------------------------------------------------------
# Directory for storing uploaded files and generated outputs
WORKSPACE_BASE_DIR=workspace

# HTTP server port for serving files
HTTP_SERVER_PORT=8100

# Backend API port
BACKEND_PORT=8200

# ------------------------------------------------------------------------------
# Optional: Logging and Monitoring
# ------------------------------------------------------------------------------
# Set to "true" to enable debug logging
# DEBUG=false

# Set to "true" to log API costs (OpenRouter only)
# LOG_COSTS=true
